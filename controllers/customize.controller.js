(function (angular) {
    'use strict';
    angular
        .module('MainApp')
        .controller('customizeController', Constructor);
    function Constructor(repositoryFactory) {
        var vm = this;

    }
})(window.angular);