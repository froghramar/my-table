(function (angular) {
    'use strict';
    angular
        .module('MainApp')
        .controller('tableController', Constructor);
    function Constructor(repositoryFactory) {
        var vm = this;
        
    }
})(window.angular);